# 🚀 Быстрый старт MVP бота

## Шаг 1: Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите имя: **My Massage Bot** (или любое другое)
4. Введите username: **my_massage_bot** (должен заканчиваться на `bot`)
5. Скопируйте токен (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Узнайте свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. Скопируйте ваш ID (например: `123456789`)

## Шаг 3: Настройте .env файл

```bash
# В директории gobot создайте файл .env
cd gobot
notepad .env  # или любой редактор
```

Вставьте и замените на свои данные:

```env
BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
ADMIN_USER_IDS=ВАШ_TELEGRAM_ID
DB_PATH=./bot.db
TIMEZONE=Europe/Moscow
BOT_DEBUG=true
```

**Пример:**
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_USER_IDS=123456789
DB_PATH=./bot.db
TIMEZONE=Europe/Moscow
BOT_DEBUG=true
```

## Шаг 4: Запустите бота

### Вариант 1: Запуск напрямую (для тестирования)

```bash
go run ./cmd/bot/main.go
```

### Вариант 2: Сборка и запуск

```bash
go build -o bot.exe ./cmd/bot
./bot.exe
```

### Вариант 3: Docker (для продакшена)

```bash
docker-compose up -d
docker-compose logs -f
```

## Шаг 5: Тестирование

### Откройте вашего бота в Telegram

1. Найдите бота по username (который вы указали в BotFather)
2. Нажмите **Start**

### Проверьте функционал:

#### Для клиентов:
1. ✅ `/start` - проверьте приветствие
2. ✅ `/book` - создайте тестовую запись:
   - Выберите услугу
   - Выберите дату
   - Выберите время
   - Подтвердите
3. ✅ `/my_bookings` - посмотрите свои записи
4. ✅ `/cancel` - попробуйте отменить запись

#### Для админов:
1. ✅ `/admin` - откройте админ-панель
2. ✅ **Все записи** - просмотрите все бронирования
3. ✅ **Услуги** - управляйте услугами:
   - Включить/выключить услугу
   - Удалить услугу
4. ✅ **Статистика** - посмотрите статистику системы

### Проверьте уведомления:

1. ✅ Создайте запись - вы должны получить:
   - Подтверждение с деталями
   - Админ получит уведомление о новой записи

2. ✅ Отмените запись - вы должны получить:
   - Уведомление об отмене
   - Админ получит уведомление об отмене

3. ✅ Напоминания (автоматические):
   - Создайте запись на завтра
   - Через некоторое время (до 1 часа) получите напоминание
   - Напоминания отправляются каждый час для записей на следующий день

## Шаг 6: Управление услугами

### Текущие услуги (создаются автоматически):

1. Классический массаж - 3000 руб., 60 мин
2. Расслабляющий массаж - 4000 руб., 90 мин
3. Спортивный массаж - 3500 руб., 60 мин
4. Депиляция ног - 2500 руб., 45 мин
5. Депиляция рук - 1500 руб., 30 мин
6. Депиляция зоны бикини - 2000 руб., 30 мин

### Добавление новых услуг (через SQL):

```bash
# Подключитесь к БД
sqlite3 bot.db

# Добавьте услугу
INSERT INTO services (name, duration, price, description, is_active, created_at, updated_at)
VALUES ('Новая услуга', 60, 250000, 'Описание услуги', 1, datetime('now'), datetime('now'));

# Выход
.quit
```

**Примечание:** Цена указывается в копейках (250000 = 2500 руб.)

## Шаг 7: Деплой на сервер

### Подготовка сервера:

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Установите Docker
sudo apt update
sudo apt install docker.io docker-compose -y
sudo systemctl enable docker
sudo systemctl start docker
```

### Деплой бота:

```bash
# Клонируйте проект
git clone <your-repo>
cd gobot

# Создайте .env
cp .env.example .env
nano .env  # отредактируйте

# Запустите
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

### Управление на сервере:

```bash
# Остановить
docker-compose stop

# Запустить
docker-compose start

# Перезапустить
docker-compose restart

# Обновить после изменений
docker-compose down
docker-compose build
docker-compose up -d
```

## 🎉 Готово!

MVP бот готов к работе! 

### Что работает:
- ✅ Запись на услуги (массаж + депиляция)
- ✅ Просмотр и отмена записей
- ✅ Админ-панель с управлением услугами
- ✅ Уведомления клиентам и админам
- ✅ Автоматические напоминания
- ✅ Статистика

### Что делать дальше:
1. Протестируйте все функции
2. Добавьте своих админов в `ADMIN_USER_IDS` (через запятую)
3. Настройте услуги под ваш салон
4. Опубликуйте бота в вашем канале
5. Соберите обратную связь от клиентов

## 🐛 Проблемы?

### Бот не запускается:
- Проверьте токен в `.env`
- Проверьте, что Go установлен: `go version`
- Проверьте логи: `docker-compose logs -f`

### Бот не отвечает:
- Убедитесь, что бот запущен
- Проверьте логи на ошибки
- Отправьте `/start` заново

### Не работают напоминания:
- Напоминания отправляются раз в час
- Создайте запись на завтра и подождите до часа
- Проверьте логи: должны быть строки "Reminder worker started"

### Нет прав админа:
- Проверьте, что ваш ID в `ADMIN_USER_IDS`
- Перезапустите бота после изменения `.env`
- Отправьте `/start` заново

## 📞 Нужна помощь?

Проверьте логи:
```bash
# Локально
# Смотрите вывод в терминале

# Docker
docker-compose logs -f bot
```

Создайте issue в репозитории с описанием проблемы и логами.


