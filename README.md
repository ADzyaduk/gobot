# 🤖 Telegram Booking Bot

Telegram бот для записи на услуги массажа и депиляции, написанный на Go.

## 📋 Возможности

### Для клиентов:
- 📝 Запись на услуги (массаж, депиляция)
- 📅 Выбор даты и времени
- 📋 Просмотр своих записей
- ❌ Отмена записей

### Для администраторов:
- 📊 Просмотр всех записей
- 📈 Статистика
- 🛠 Управление услугами
- ⏰ Управление временными слотами

## 🚀 Быстрый старт

### Требования
- Go 1.21+ (для локальной разработки)
- Docker и Docker Compose (для деплоя)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "My Massage Bot")
   - Введите username бота (должен заканчиваться на "bot", например: "my_massage_bot")
4. Сохраните полученный токен

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo>
cd gobot
```

2. **Создайте файл `.env` из примера:**
```bash
cp .env.example .env
```

3. **Отредактируйте `.env` и добавьте свои данные:**
```env
BOT_TOKEN=your_actual_bot_token_here
ADMIN_USER_IDS=your_telegram_user_id
DB_PATH=./bot.db
TIMEZONE=Europe/Moscow
BOT_DEBUG=true
```

**Как узнать свой Telegram User ID:**
- Отправьте любое сообщение боту [@userinfobot](https://t.me/userinfobot)
- Он ответит вашим ID

4. **Установите зависимости:**
```bash
go mod download
```

5. **Запустите бота:**
```bash
go run ./cmd/bot/main.go
```

### Деплой с Docker

1. **Создайте `.env` файл:**
```bash
cp .env.example .env
# Отредактируйте .env с вашими данными
```

2. **Запустите с Docker Compose:**
```bash
docker-compose up -d
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка бота:**
```bash
docker-compose down
```

### Деплой на сервер

1. **Подключитесь к серверу:**
```bash
ssh user@your-server.com
```

2. **Установите Docker и Docker Compose** (если еще не установлены):
```bash
# Для Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose -y
sudo systemctl enable docker
sudo systemctl start docker
```

3. **Клонируйте проект:**
```bash
git clone <your-repo>
cd gobot
```

4. **Настройте `.env`:**
```bash
cp .env.example .env
nano .env  # или vim .env
```

5. **Запустите бота:**
```bash
docker-compose up -d
```

6. **Проверьте статус:**
```bash
docker-compose ps
docker-compose logs -f bot
```

## 📝 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательна | По умолчанию |
|-----------|----------|-------------|--------------|
| `BOT_TOKEN` | Telegram Bot Token | ✅ Да | - |
| `ADMIN_USER_IDS` | ID админов (через запятую) | ❌ Нет | - |
| `DB_PATH` | Путь к файлу БД | ❌ Нет | `./bot.db` |
| `TIMEZONE` | Часовой пояс | ❌ Нет | `UTC` |
| `BOT_DEBUG` | Режим отладки | ❌ Нет | `false` |

### Первый запуск

При первом запуске бот автоматически создаст:
- База данных SQLite
- Начальный набор услуг:
  - Классический массаж (3000 руб.)
  - Расслабляющий массаж (4000 руб.)
  - Спортивный массаж (3500 руб.)
  - Депиляция ног (2500 руб.)
  - Депиляция рук (1500 руб.)
  - Депиляция зоны бикини (2000 руб.)

## 🎮 Использование

### Команды бота

**Для всех пользователей:**
- `/start` - Начать работу с ботом
- `/help` - Справка по командам
- `/book` - Записаться на услугу
- `/my_bookings` - Посмотреть свои записи
- `/cancel` - Отменить запись

**Для администраторов:**
- `/admin` - Админ-панель
  - 📋 Все записи
  - 📊 Статистика
  - 🛠 Услуги (в разработке)
  - ⏰ Временные слоты (в разработке)

### Процесс записи

1. Отправьте `/book`
2. Выберите услугу
3. Выберите дату
4. Выберите время
5. Подтвердите запись
6. Получите подтверждение

## 🛠 Разработка

### Структура проекта

```
gobot/
├── cmd/
│   └── bot/
│       └── main.go           # Точка входа
├── internal/
│   ├── bot/
│   │   ├── bot.go           # Основная логика бота
│   │   ├── handlers.go      # Обработчики команд
│   │   ├── callbacks.go     # Обработчики callback
│   │   └── keyboards.go     # Клавиатуры
│   ├── config/
│   │   └── config.go        # Конфигурация
│   ├── database/
│   │   ├── db.go           # Инициализация БД
│   │   └── models.go       # Модели данных
│   └── services/
│       ├── booking.go      # Логика бронирования
│       └── user.go         # Логика пользователей
├── .env.example
├── .gitignore
├── docker-compose.yml
├── Dockerfile
├── go.mod
├── go.sum
└── README.md
```

### Зависимости

- [telebot/v3](https://github.com/tucnak/telebot) - Telegram Bot API
- [GORM](https://gorm.io/) - ORM для Go
- [SQLite](https://www.sqlite.org/) - База данных
- [godotenv](https://github.com/joho/godotenv) - Загрузка .env файлов

### Сборка

```bash
# Локальная сборка
go build -o bot ./cmd/bot

# Сборка для Linux (с Windows)
GOOS=linux GOARCH=amd64 go build -o bot ./cmd/bot

# Сборка Docker образа
docker build -t massage-bot .
```

### Тестирование

```bash
# Запуск тестов
go test ./...

# С покрытием
go test -cover ./...

# Подробный вывод
go test -v ./...
```

## 📊 База данных

Бот использует SQLite с GORM. Схема БД включает:

- **Users** - Пользователи Telegram
- **Services** - Услуги (массаж, депиляция)
- **Bookings** - Записи клиентов
- **TimeSlots** - Временные слоты (планируется)

## 🔒 Безопасность

- ✅ Токен бота хранится в переменных окружения
- ✅ Проверка прав доступа для админ-команд
- ✅ Валидация пользовательских данных
- ✅ Использование context.Context для таймаутов

## ✨ Новые возможности MVP

### Реализовано:
- ✅ Полное управление услугами через админ-панель (включение/отключение, удаление)
- ✅ Система уведомлений (подтверждение записи, отмены)
- ✅ Автоматические напоминания за день до записи
- ✅ Уведомления админов о новых записях и отменах

### Как работают уведомления:
1. **При создании записи:**
   - Клиент получает подтверждение с деталями
   - Все админы получают уведомление о новой записи

2. **При отмене записи:**
   - Клиент получает уведомление об отмене
   - Все админы получают уведомление об отмене

3. **Напоминания:**
   - Бот автоматически проверяет записи каждый час
   - За день до визита клиент получает напоминание
   - Работает в фоновом режиме

## 🚧 Roadmap

- [ ] Управление временными слотами через админ-панель
- [ ] Добавление новых услуг через бота (сейчас через БД)
- [ ] Интеграция с календарем
- [ ] Оплата онлайн
- [ ] Отзывы и рейтинги
- [ ] Генерация контента для канала (AI)

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте, что `.env` настроен правильно
2. Проверьте логи: `docker-compose logs -f`
3. Убедитесь, что бот имеет правильный токен
4. Проверьте, что ваш User ID добавлен в `ADMIN_USER_IDS` для доступа к админ-панели

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано с ❤️ для салона красоты

