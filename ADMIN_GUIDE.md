# 📘 Руководство по управлению ботом

## 🔐 Доступ к админ-панели

Только пользователи с ID, указанными в `.env` файле (`ADMIN_USER_IDS`), имеют доступ к админ-панели.

Команда: `/admin`

## 🛠 Управление услугами

### Просмотр всех услуг

1. `/admin` → **Услуги**
2. Вы увидите список всех услуг с их статусом (✅ активна, ❌ неактивна)

### Редактирование услуги

1. `/admin` → **Услуги** → Выберите услугу
2. Выберите что хотите изменить:
   - **📝 Изменить название** - новое имя услуги
   - **💰 Изменить цену** - введите число в рублях (например: 2500)
   - **⏱ Изменить длительность** - введите число в минутах (например: 60)
   - **📝 Изменить описание** - новый текст описания
   - **🔄 Вкл/Выкл** - активировать/деактивировать услугу
   - **🗑 Удалить** - удалить услугу (восстановить нельзя!)

### Добавление новой услуги

1. `/admin` → **Услуги** → **➕ Добавить услугу**
2. Пошаговый процесс:
   - **Шаг 1:** Введите название (например: "Классический массаж спины")
   - **Шаг 2:** Введите цену в рублях (например: 2500)
   - **Шаг 3:** Введите длительность в минутах (например: 60)
   - **Шаг 4:** Введите описание (например: "Расслабляющий массаж спины и шеи")

### Включение/отключение услуги

Отключенные услуги не будут видны клиентам при записи, но все существующие записи сохранятся.

1. `/admin` → **Услуги** → Выберите услугу → **🔄 Вкл/Выкл**

### Удаление услуги

⚠️ **Внимание:** Удаление нельзя отменить!

1. `/admin` → **Услуги** → Выберите услугу → **🗑 Удалить**

## 💰 Управление скидками и акциями

### Создание акции

**Через код (будет добавлено в UI позже):**

```go
// Пример создания скидки 20% на массаж с 1 по 31 декабря
discountService := services.NewDiscountService()
discount, err := discountService.CreateDiscount(
    ctx,
    serviceID,                    // ID услуги
    "Новогодняя акция",          // Название акции
    20,                          // Процент скидки
    time.Date(2024, 12, 1, 0, 0, 0, 0, time.UTC),  // Начало
    time.Date(2024, 12, 31, 23, 59, 59, 0, time.UTC), // Конец
)
```

### Как работают скидки

- Скидки применяются автоматически в указанный период
- Клиенты видят сниженную цену при выборе услуги
- Можно создать несколько акций на разные услуги
- Активные акции применяются только к новым записям

### Просмотр активных акций

**Через SQL:**
```bash
sqlite3 bot.db
SELECT s.name, d.name as discount_name, d.percentage, d.start_date, d.end_date, d.is_active
FROM discounts d
JOIN services s ON d.service_id = s.id
WHERE d.is_active = 1;
```

## 📊 Статистика

1. `/admin` → **Статистика**

Показывает:
- 👥 Всего пользователей
- 📋 Всего записей
- ✅ Активных записей (ожидающие + подтвержденные)
- ✔️ Завершенных записей
- 🛠 Активных услуг

## 📅 Просмотр всех записей

1. `/admin` → **Все записи**

Показывает последние 15 записей с информацией:
- Статус (⏳ ожидает, ✅ подтверждено, ❌ отменено, ✔️ завершено)
- Название услуги
- Данные клиента (имя, username)
- Дата и время
- Стоимость

## 🔔 Уведомления для админов

Вы будете автоматически получать уведомления о:
- 🔔 Новых записях (с данными клиента и деталями)
- ❌ Отменах записей клиентами

## ⏰ Управление временными слотами

### Текущая настройка

По умолчанию доступны слоты:
- **Время:** 09:00 - 20:00 (каждый час)
- **Дни:** Следующие 7 дней от сегодня

### Настройка рабочего расписания (будет добавлено)

Планируется добавить через админ-панель:
- Установка рабочих дней и часов
- Блокировка определенных дат (выходные, праздники)
- Настройка интервалов между слотами

## 💡 Советы по управлению

### Управление ценами

**Регулярные цены:**
- Редактируйте через админ-панель
- Изменения видны сразу для новых записей

**Временные акции:**
- Используйте систему скидок
- Укажите срок действия
- Скидка применится автоматически

### Управление загрузкой

**Если много записей:**
- Добавьте новые услуги
- Увеличьте временные слоты
- Настройте длительность услуг

**Если мало записей:**
- Создайте акцию со скидкой
- Добавьте более популярные услуги
- Улучшите описания услуг

### Работа с клиентами

**При создании записи:**
- Клиент получает автоматическое подтверждение
- Вы получаете уведомление с данными клиента

**За день до записи:**
- Клиент получает напоминание автоматически
- Вам не нужно ничего делать

**При отмене:**
- И клиент, и вы получите уведомление
- Слот снова становится доступным

## 🗄 Прямой доступ к базе данных

Если нужно что-то сделать напрямую через SQL:

```bash
# Подключиться к базе
sqlite3 bot.db

# Просмотреть все услуги
SELECT * FROM services;

# Изменить цену услуги
UPDATE services SET price = 300000 WHERE id = 1;

# Добавить скидку
INSERT INTO discounts (service_id, name, percentage, start_date, end_date, is_active, created_at, updated_at)
VALUES (1, 'Летняя акция', 15, '2024-06-01', '2024-08-31', 1, datetime('now'), datetime('now'));

# Выход
.quit
```

**Примечание:** Цены в БД хранятся в копейках (300000 = 3000 руб.)

## 🔧 Расширенная настройка

### Добавление админов

Отредактируйте `.env`:
```env
ADMIN_USER_IDS=123456789,987654321,111222333
```

Перезапустите бота:
```bash
# Локально
# Просто перезапустите

# Docker
docker-compose restart
```

### Изменение часового пояса

Отредактируйте `.env`:
```env
TIMEZONE=Asia/Almaty
```

### Резервное копирование

**Ручное:**
```bash
cp bot.db bot_backup_$(date +%Y%m%d).db
```

**Автоматическое (добавьте в cron):**
```bash
0 2 * * * cp /path/to/bot.db /path/to/backups/bot_$(date +\%Y\%m\%d).db
```

## 📞 Получение ID пользователя

Если нужно добавить нового админа:

1. Попросите его написать боту [@userinfobot](https://t.me/userinfobot)
2. Бот ответит с его ID
3. Добавьте ID в `.env` в `ADMIN_USER_IDS`
4. Перезапустите бота

## ❓ Частые вопросы

**Q: Можно ли восстановить удаленную услугу?**  
A: Нет, но можно создать новую с теми же данными.

**Q: Видят ли клиенты неактивные услуги?**  
A: Нет, только админы видят все услуги.

**Q: Можно ли отменить запись от имени клиента?**  
A: Да, через прямой доступ к БД (`UPDATE bookings SET status = 'cancelled' WHERE id = X`).

**Q: Как изменить время работы бота?**  
A: Сейчас только через код (`keyboards.go`). В будущем будет через админ-панель.

**Q: Можно ли создать акцию "2 по цене 1"?**  
A: Пока нет, только процентные скидки. Можно установить скидку 50% как вариант.

## 🚀 Что дальше

Планируется добавить:
- UI для создания акций/скидок через бота
- Управление временными слотами через админ-панель
- Редактирование записей (изменение даты/времени)
- Экспорт отчетов
- Статистика по услугам (популярность)
- Графики и диаграммы

## 📧 Обратная связь

Если у вас есть идеи по улучшению админ-панели, создайте issue в репозитории или обратитесь к разработчику.


